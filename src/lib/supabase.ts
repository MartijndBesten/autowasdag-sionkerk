// Dit bestand is vervangen door src/lib/supabase/ (map).
// Gebruik:
//   import { createClient } from "@/lib/supabase/client"  ← browser
//   import { createClient } from "@/lib/supabase/server"  ← server
//   import { createAdminClient } from "@/lib/supabase/admin"  ← service role (server only)
//
// Dit bestand bestaat alleen nog voor eventuele restimports — verwijder het als je zeker bent.
export {};
